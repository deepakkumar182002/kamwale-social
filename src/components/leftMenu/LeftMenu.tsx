const LeftMenu = ({ type }: { type: "home" | "profile" }) => {
  return (
    <div className="flex flex-col gap-6">
      {/* Left menu removed - all navigation moved to Sidebar */}
    </div>
  );
};

export default LeftMenu;
